import { Manrope } from "next/font/google";
import { Inter } from "next/font/google";

export const manrope = Manrope({ subsets: ['cyrillic', 'latin'], weight: ['400', '700'] });
export const inter = Inter({ subsets: ['cyrillic', 'latin'], weight: ['400', '700', '800'] });
